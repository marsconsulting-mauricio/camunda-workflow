server:
  port: 8080

spring:
  datasource:
    url: jdbc:h2:file:./camunda-h2-database
    driver-class-name: org.h2.Driver
    username: sa
    password:
  h2:
    console:
      enabled: true

camunda:
  bpm:
    admin-user:
      id: admin
      password: admin